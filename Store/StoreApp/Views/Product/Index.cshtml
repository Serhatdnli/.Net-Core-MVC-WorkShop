
@model List<Product>

@foreach (var product in Model)
{
    <div>
        <h3>@product.ProductName</h3>
        <p>@product.Price </p>
        <p>
            <a href="/Product/Get/@product.Id">Get Details</a>
        </p>
    </div>
}